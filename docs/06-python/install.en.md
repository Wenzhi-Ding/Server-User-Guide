I suggest using uv to [install](../02-uv/install.md).

??? question "What should I do if I encounter `ModuleNotFoundError`?"
    
    Go back to the Linux terminal and use `uv add xxx` (recommended) or `uv pip install xxx` to install the package.